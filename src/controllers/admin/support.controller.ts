import { Request, Response } from "express";

export async function listTickets(_req: Request, res: Response) {
  return res.json({ status: "under_construction" });
}

export async function getTicket(_req: Request, res: Response) {
  return res.json({ status: "under_construction" });
}

export async function updateTicketStatus(_req: Request, res: Response) {
  return res.json({ status: "under_construction" });
}

export async function deleteTicket(_req: Request, res: Response) {
  return res.json({ status: "under_construction" });
}
